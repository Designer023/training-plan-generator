import {EffortTable} from "./index"

describe( '21km to 50km', () => {
    
    describe( 'Progress', () => {
        test('Plan length should be 23', () => {
            expect(1).toBe(1)
        });
    })
})